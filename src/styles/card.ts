/**
 * The shared surface style for content cards — About's tiles and toolkit
 * panels, Career's tab container, and the Portfolio stage panel.
 *
 * Matches the Figma spec: #15161A fill, #89919F stroke, 10px radius, and a
 * background blur so the page grid reads faintly through it.
 */
export const CARD = 'rounded-[10px] border border-[#89919F] bg-[#15161A] backdrop-blur-md';
